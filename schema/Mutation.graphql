type Mutation {
  novoUsuario(
    dados: UsuarioInput!
  ): Usuario!

  excluirUsuario(id: Int): Usuario

  alterarUsuario(
    id: Int
    nome: String  # Virgula entre os parâmetros é opcional
    email: String
    idade: Int
  ): Usuario
}